I am the transpiler driver that finds all the required classes and methods and does type inference on them.

I am abstract because we can't compile the whole image, so subclasses must provide limits.
